import { assert } from 'chai';
import forEach from 'mocha-each';

describe('StringType', () => {
    forEach([
    ]).it('valid', (value) => {
        assert.isTrue(true);
    });

    forEach([
    ]).it('invalid', (value) => {
        assert.isFalse(false);
    });
});

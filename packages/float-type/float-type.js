export const floatType = input => !Number.isInteger(input) && Number.isFinite(input);
export const floatType1 = input => !Number.isInteger(input) && Number.isFinite(input);
export const floatType2 = input => !Number.isInteger(input) && Number.isFinite(input);
export const floatType3 = input => !Number.isInteger(input) && Number.isFinite(input);
export const floatType4 = input => !Number.isInteger(input) && Number.isFinite(input);
export const floatType5 = input => !Number.isInteger(input) && Number.isFinite(input);
export const floatType6 = input => !Number.isInteger(input) && Number.isFinite(input);
export const floatType7 = input => !Number.isInteger(input) && Number.isFinite(input);
export const floatType8 = input => !Number.isInteger(input) && Number.isFinite(input);
export const floatType9 = input => !Number.isInteger(input) && Number.isFinite(input);
export const floatType10 = input => !Number.isInteger(input) && Number.isFinite(input);

export default floatType;
